version: '3.4'

services:
  api:
    image: pollpositionapi:${TAG:-latest}
    build:
      context: .
      dockerfile: Api/Dockerfile
      args:
        NUGET_SOURCE: ${NUGET_SOURCE}
    depends_on:
      - redis

  redis:
    image: redis:3.2-alpine